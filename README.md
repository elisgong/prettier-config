# `@elisgong/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @elisgong/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@elisgong/prettier-config"
}
```
